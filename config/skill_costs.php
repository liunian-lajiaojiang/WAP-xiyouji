<?php
/**
 * 技能与修炼配置
 * 
 * 将 exert.php, enforce.php, mo.php, sleep.php 等中的技能消耗/冷却数值集中管理。
 */

return [
    // ========================
    // 静心诀 (jingxinjue)
    // ========================
    'jingxinjue' => [
        'required_force' => 200,   // 需要内力
        'force_cost'     => 100,   // 内力消耗
    ],

    // ========================
    // 护体真气 (huti)
    // ========================
    'huti' => [
        'required_force' => 100,   // 需要内力
        'required_sen'   => 100,   // 需要精神
        'force_cost'     => 100,   // 内力消耗
        'sen_cost'       => 100,   // 精神消耗
    ],

    // ========================
    // 通用技能消耗（其他 exert 技能）
    // ========================
    'generic_exert' => [
        'required_force' => 100,   // 需要内力
        'force_cost'     => 100,   // 内力消耗
    ],

    // ========================
    // 加力范围 (enforce)
    // ========================
    'enforce' => [
        'min' => 0,
        'max' => 100,
    ],

    // ========================
    // 摩经消耗 (mo.php)
    // ========================
    'mo' => [
        'required_jing' => 110,    // 需要精力
        'jing_cost'     => 100,    // 精力消耗
    ],

    // ========================
    // 睡眠冷却 (sleep.php)
    // ========================
    'sleep' => [
        'cooldown_seconds' => 90,  // 睡眠冷却时间
    ],

    // ========================
    // 逃跑概率 (out.php)
    // ========================
    'escape' => [
        'base_chance'     => 50,   // 基础逃跑成功率(%)
        'dex_percent'     => 0.5,  // 每点身法增加的百分比
    ],

    // ========================
    // 投降/屈服概率 (surrender.php)
    // ========================
    'surrender' => [
        'base_reject_chance' => 30,  // 基础拒绝率(%)
    ],

    // ========================
    // 切磋邀请 (fight.php)
    // ========================
    'spar_invite' => [
        'expire_seconds'    => 30,   // 邀请过期秒数
        'reject_cooldown'   => 30,   // NPC拒绝后冷却秒数
    ],

    // ========================
    // 死亡/灵魂相关
    // ========================
    'death' => [
        'soul_duration_seconds' => 600,   // 灵魂状态持续秒数(10分钟)
        'revive_force_percent'  => 50,    // 复活后内力恢复百分比
        'revive_kee_percent'    => 50,    // 复活后气血恢复百分比
    ],
];
